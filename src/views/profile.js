import React from 'react'
import profileStyles from './profile.module.css'

const Profile = () => {
  // needs user, name, picture, email
  return (
    <div className={profileStyles.circle}>
        DT
      <img></img>
    </div>
  )
}

export default Profile
